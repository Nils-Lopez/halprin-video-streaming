import styled from '@emotion/styled';

export const TopBar = styled.div`
  .right {
    width: 250px;
    .dot {
      margin-right: 10px;
      &:hover {
        filter: brightness(80%);
      }
    }
  }
`;
